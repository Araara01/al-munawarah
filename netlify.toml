[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Vite single-page app: serve the app shell for client-side routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

